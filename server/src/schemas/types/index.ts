export enum UserStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
}
export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}
export enum MessageType {
  SYSTEM = 'system',
  TEXT = 'text',
  IMAGE = 'image',
  DOC = 'doc',
  VOICE = 'voice',
  TIMELINE = 'timeline',
  CONTACT = 'contact',
}
